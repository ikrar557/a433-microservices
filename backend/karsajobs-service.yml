apiVersion: v1
kind: Service
metadata:
  name: karsajobs
  namespace: deployments
  labels:
    app: karsajobs
spec:
  type: NodePort
  ports:
  - port: 8080
    targetPort: 8080
    nodePort: 30081
  selector:
    app: karsajobs

  