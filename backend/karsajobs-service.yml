apiVersion: v1
kind: Service
metadata:
  name: karsajobs
  namespace: deployments
  labels:
    app: karsajobs
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
  selector:
    app: karsajobs

  