apiVersion: v1
kind: Service
metadata:
  name: shippingservice
  namespace: deployments
spec:
  selector:
    app: shippingservice
  ports:
  - port: 3001
    targetPort: 3001
    nodePort: 30001
  type: NodePort