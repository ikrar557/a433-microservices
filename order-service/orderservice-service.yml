apiVersion: v1
kind: Service
metadata:
  name: orderservice
  namespace: deployments
spec:
  selector:
    app: orderservice
  ports:
  - port: 3000
    targetPort: 3000
    nodePort: 30000
  type: NodePort